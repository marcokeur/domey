extends layout

block head
    link(href='/assets/css/table.css', rel='stylesheet')

block content
    .container
        .row
            table(id="scenesTable" style="width: 100%")
                thead
                    tr
                        th Scene
                        th Icon
                        th Enable
                        th Disable
                        th Active
                tbody
    br

block script
    script.
        $(document).ready(function(){
            loadScenes('#scenesTable', function(){ console.log('loading done'); });
            //openWebSocketConnection();
        });
    script(src='/js/dashboard.js')
    script(src='/js/dashboard_scenes.js')
    script(src="https://cdn.socket.io/socket.io-1.3.7.js")
